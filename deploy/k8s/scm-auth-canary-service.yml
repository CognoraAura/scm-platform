apiVersion: v1
kind: Service
metadata:
  name: scm-auth-canary
  namespace: scm-prod
spec:
  selector:
    app: scm-auth
    track: canary
  ports:
    - port: 8106
      targetPort: 8106
  type: ClusterIP